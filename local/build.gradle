task copyApp(type: Copy, dependsOn: build) {
    from("${buildDir}/libs") {
        include "*-all.jar"
        rename { "TemplateService.jar" }
    }
    from(fileTree("${projectDir}/config/").files) {
        include "**/*.yml"
    }
    into("${rootDir}/local")
}